shader_custom_init()

/*
var ox = x + sprite_width/2
var oy = y
var dx = self.delta_position.x
var dy = self.delta_position.y
var line_width = 6

draw_line_width(ox, oy, ox + dx, oy + dy, line_width)
*/

draw_self();

shader_reset()